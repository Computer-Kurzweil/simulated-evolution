<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.woehlke.computer.kurzweil</groupId>
        <artifactId>computer-kurzweil-parent</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>simulatedevolution</artifactId>
    <version>3.0.10-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>Simulated Evolution</name>
    <description>Artificial Life Simulation of Bacteria Motion depending on DNA</description>
    <url>https://java.woehlke.org/simulated-evolution</url>
    <inceptionYear>2013</inceptionYear>

    <scm>
        <connection>scm:git:git@github.com:Computer-Kurzweil/simulated-evolution.git</connection>
        <developerConnection>scm:git:git@github.com:Computer-Kurzweil/simulated-evolution.git</developerConnection>
        <url>https://github.com/Computer-Kurzweil/simulated-evolution/tree/${project.scm.tag}</url>
        <tag>HEAD</tag>
    </scm>

    <issueManagement>
        <url>https://github.com/Computer-Kurzweil/simulated-evolution/issues/</url>
        <system>GitHub</system>
    </issueManagement>

    <distributionManagement>
        <site>
            <id>simulatedevolution.local</id>
            <url>file://${user.home}/public_html/java/simulated-evolution/</url>
        </site>
        <snapshotRepository>
            <id>snapshots</id>
            <name>tw-thinkpad-snapshots</name>
            <url>file://${user.home}/public_html/java/libs-snapshots-local/</url>
        </snapshotRepository>
        <repository>
            <id>central</id>
            <name>tw-thinkpad-releases</name>
            <url>file://${user.home}/public_html/java/libs-release-local/</url>
        </repository>
    </distributionManagement>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0</url>
        </license>
    </licenses>

    <repositories>
        <repository>
            <id>libs-release-local-woehlke-org</id>
            <name>woehlke.org</name>
            <url>https://java.woehlke.org/libs-release-local/</url>
        </repository>
    </repositories>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <configuration>
                    <mainClass>org.woehlke.computer.kurzweil.simulated.evolution.SimulatedEvolutionApplication</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>run</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <skipTests>false</skipTests>
            </properties>
            <build>
                <finalName>${project.artifactId}</finalName>
                <defaultGoal>clean install exec:java</defaultGoal>
            </build>
        </profile>
        <profile>
            <id>build</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <skipTests>false</skipTests>
            </properties>
            <build>
                <finalName>${project.artifactId}</finalName>
                <defaultGoal>
                    clean dependency:resolve dependency:resolve-plugins dependency:sources install site site:deploy exec:java
                </defaultGoal>
            </build>
        </profile>
    </profiles>
</project>
